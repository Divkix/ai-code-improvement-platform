########################################################
#  FRONTEND  (.env in /frontend   — Vite/SvelteKit)     #
########################################################

# Base URL of the backend (no trailing slash)
VITE_API_URL=http://localhost:8080

# ---- Optional public variables (must start with PUBLIC_) ----
# PUBLIC_APP_VERSION=1.2.0
